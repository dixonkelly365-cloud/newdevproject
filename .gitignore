# Secrets — never commit your real .env, only .env.example
.env

# Dependencies
node_modules/

# Generated output (regenerated fresh each run — no need to version-control it)
output/

# OS/editor cruft
.DS_Store
*.log
